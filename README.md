# ADMS-ERP.NET
ADMS ERP is a .NET MVC web application for managing and processing items with weights in a parent–child hierarchy. Items can be processed into multiple child items using recursive logic and stored in a self-referencing SQL Server database. The system includes CRUD operations, validation, and a tree view visualization.
